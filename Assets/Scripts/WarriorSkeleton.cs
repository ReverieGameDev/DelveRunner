using UnityEngine;

public class WarriorSkeleton : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //charge attack
    //in combination with the charge attack, the warrior then hits you with another ability like ambessa who charges in then uses an aoe attack then goes back to original position
    //increase armor, shield 
    //summoner summons and buffs AS
}
