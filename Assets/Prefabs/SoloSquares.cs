using UnityEngine;

public class SoloSquares : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public bool squareOccupied = false;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
