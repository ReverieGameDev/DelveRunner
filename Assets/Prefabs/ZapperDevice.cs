using System.Collections;
using UnityEngine;

public class ZapperDevice : MonoBehaviour
{
    public int zapperCounter = 0;
    private float zapChargeInterval = 1f;
    public float zapChargeIntervalInterrupt = 3f;
    public bool zapInterrupted = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void InterruptEnvironmentThreat()
    {

    }

}
